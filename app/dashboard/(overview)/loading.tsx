// este componente se carga automaticamente cuando se esta cargando la data en la pagina
import DashboardSkeleton from '@/app/ui/skeletons';
 
export default function Loading() {
  return <DashboardSkeleton />;
}